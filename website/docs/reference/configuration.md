# Configuration Reference

This page documents all configuration files and their options.

## config.yml

The main configuration file shared by all servers and services.

**Location**: `configuration/config.yml`

```yml
host-name: 0.0.0.0
mongodb: mongodb://localhost
redis-uri: redis://localhost:6379
velocity-secret: your-forwarding-secret-here
require-auth: false
integrations:
    spark: false
    anticheat: false
    via-version: false
    sentry-dsn: ''
limbo:
    host-name: 127.0.0.1
    port: 65535
resource-packs: { }
```

### Fields

| Field             | Required | Description                                  |
|-------------------|----------|----------------------------------------------|
| `host-name`       | Yes      | Address to bind the server to                |
| `mongodb`         | Yes      | MongoDB connection string                    |
| `redis-uri`       | Yes      | Redis connection string                      |
| `velocity-secret` | Yes      | Must match `forwarding.secret` from Velocity |
| `require-auth`    | No       | Require authentication on initial connection |
| `integrations.spark`      | No       | Enable Spark performance monitoring |
| `integrations.anticheat`  | No       | Enable anti-cheat measures          |
| `integrations.via-version`| No       | Support multiple Minecraft versions (may cause issues) |
| `integrations.sentry-dsn` | No       | Sentry error tracking DSN (empty = disabled) |
| `limbo.host-name` / `limbo.port` | No | PicoLimbo connection details   |
| `resource-packs`  | No       | Resource pack server URLs keyed by pack name |

### Docker Configuration

For Docker deployments, use `config.docker.yml`:

```yml
velocity-secret: "docker-secret"
mongodb: "mongodb://mongodb"
redis-uri: "redis://redis:6379"
...
```

Note the hostnames `mongodb` and `redis` match Docker service names.

## velocity.toml

Velocity proxy configuration.

**Location**: `velocity.toml` (proxy directory)

### Key Settings

```toml
# Bind address
bind = "0.0.0.0:25565"

# Online mode (true = premium only, false = cracked)
online-mode = true

# Player forwarding mode (must be "modern")
player-info-forwarding-mode = "modern"

# Max players
show-max-players = 500

# MOTD
motd = "<green>Hypixel</green>"
```

### Server Registration

Servers register dynamically via Redis, so you don't need to manually configure server entries.

## PicoLimbo server.toml

Configuration for the limbo server.

**Location**: `server.toml` (PicoLimbo directory)

### Key Settings

```toml
[forwarding]
method = "MODERN"
secret = "${FORWARDING_SECRET}"
```

:::alert warning
The `secret` must match your Velocity forwarding secret exactly.
:::

## Directory Structure

### Proxy Directory

```
proxy/
├── velocity-3.5.0-SNAPSHOT-605.jar
├── velocity.toml
├── forwarding.secret          # Generated by Velocity
├── configuration/
│   └── config.yml
└── plugins/
    └── SkyBlockProxy.jar
```

### Game Server Directory

```
gameserver/
├── HypixelCore.jar
└── configuration/
    ├── config.yml
    ├── skyblock/
    │   ├── collections/
    │   ├── items/
    │   ├── levels/
    │   ├── reforges/
    │   ├── skills/
    │   ├── Minestom.fairysouls.yml
    │   ├── Minestom.regions.yml
    │   ├── Minestom.crystals.yml
    │   ├── pack_textures/    # Optional
    │   └── songs/            # Optional
    ├── bedwars/              # BedWars maps (.polar) + maps.json
    ├── murdermystery/        # Murder Mystery maps (.polar) + maps.json
    ├── skywars/              # SkyWars maps (.polar) + map configs (.json)
    ├── ravengard/            # Ravengard dungeon worlds + data
    ├── resourcepacks/        # Resource packs (e.g. ravengard-original.zip)
    └── world/                # All lobby and SkyBlock worlds (.polar)
```

See [Game Servers](/docs/setup/game-servers) for the same tree with each world file named.

### Service Directory

```
services/
├── ServiceAPI.jar
├── ServiceAuctionHouse.jar
├── ServiceBazaar.jar
├── ...
└── configuration/
    └── config.yml
```

## Environment Variables

Every `config.yml` field can be overridden by an environment variable. configlib is configured with the prefix `HYPIXEL_`, then joins the prefix and the key path with `_` and upper-cases the result — so the variable name carries a double underscore after `HYPIXEL`, and any dashes in the key itself are kept:

| Variable                            | Overrides                  |
|-------------------------------------|----------------------------|
| `HYPIXEL__HOST-NAME`                | `host-name`                |
| `HYPIXEL__MONGODB`                  | `mongodb`                  |
| `HYPIXEL__REDIS-URI`                | `redis-uri`                |
| `HYPIXEL__VELOCITY-SECRET`          | `velocity-secret`          |
| `HYPIXEL__LIMBO_PORT`               | `limbo.port`               |
| `HYPIXEL__INTEGRATIONS_VIA-VERSION` | `integrations.via-version` |

The API service port is not a config field, so there is no environment variable for it. Pass it on the command line instead:

```bash
java -jar ServiceAPI.jar --port=8081
```

## Database Collections

MongoDB collections created automatically:

| Collection              | Purpose                  |
|-------------------------|--------------------------|
| `authentication`        | Login sessions           |
| `attribute`             | Server-wide SkyBlock attributes |
| `bedwars_stat_events`   | BedWars stat events for leaderboards |
| `active-auctions`       | Active auctions          |
| `inactive-auctions`     | Completed auctions       |
| `bazaarOrders`          | Bazaar buy/sell orders   |
| `pendingTransactions`   | Bazaar pending transactions |
| `tracked-items`         | Item tracking            |
| `friend-data`           | Friend lists             |
| `pending-friend-requests` | Pending friend requests |
| `elections`             | Election definitions     |
| `election-votes`        | Cast votes               |
| `election-tallies`      | Vote tallies             |
| `guilds`                | Guild definitions        |
| `player-guilds`         | Player guild membership  |
| `punishments`           | Punishment records       |
| `replays`               | Replay metadata          |
| `replay_data`           | Recorded replay data     |
| `replay_maps`           | Uploaded replay maps     |
| `store_purchases`       | Store checkout state     |
| `stripe_events`         | Stripe webhook records   |
| `store-player-entitlements` | Per-player store awards |
| `ravengard_profiles`    | Ravengard player profiles |
| `ravengard_regions`     | Ravengard region data    |
| `ravengard_tracked_items` | Ravengard tracked items |
| `api-key`               | API keys                 |
| `api-admin`             | Admin sessions           |
| `api-request-counts`    | API rate limit counters  |

Player accounts and SkyBlock profiles are not stored in MongoDB. They live in Redis under the `hsb:acct` and `hsb:prof` prefixes.
